import SIGN_OUT from 'actionsTypes'
import R from 'ramda'

const initialState = {

}

export default (state = initialState, {type, hideButton}) => {
    if(type === 'AUTH'){

        //Когда уберется релоад страниц это убрать
        ///////////////////////////////////////////
        //localStorage['do-go'] = JSON.stringify({UserId : UserId})
       // window.location = "/"
        ///////////////////////////////////////////

        return R.merge(state, {
            hideButton : hideButton,
            UserId : UserId
        })
    }
    else{
        return state
    }
}